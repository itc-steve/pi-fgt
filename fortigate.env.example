# Copy to ~/.pi/agent/fortigate.env  (next to fortigate.json)
# chmod 600 ~/.pi/agent/fortigate.env
# Keys must match each device's "tokenEnv" in fortigate.json.
# Never commit real tokens.

FORTIGATE_EDGE_TOKEN=replace-with-rest-api-token
# FORTIGATE_DC_TOKEN=another-device-token
